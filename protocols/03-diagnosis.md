# Diagnosis protocol

Localize failures in order: corpus/ingestion, candidate retrieval, ranking,
context assembly, generation, citations/abstention, and operational limits.
Inspect per-query errors and slices, not only macro averages. State evidence,
inference, and uncertainty separately. A suspected cause is not a diagnosis
until a controlled experiment can falsify it.
